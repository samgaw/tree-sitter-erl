================================================================================
empty map
================================================================================

#{}.

--------------------------------------------------------------------------------

(source_file
  (map_expr))

================================================================================
single entry map
================================================================================

#{name => "Alice"}.

--------------------------------------------------------------------------------

(source_file
  (map_expr
    (map_field
      (atom)
      (string))))

================================================================================
multiple entry map
================================================================================

#{name => adam, age => 24, date => {july, 29}}.

--------------------------------------------------------------------------------

(source_file
  (map_expr
    (map_field
      (atom)
      (atom))
    (map_field
      (atom)
      (integer))
    (map_field
      (atom)
      (tuple
        (atom)
        (integer)))))

================================================================================
map with integer keys
================================================================================

#{1 => "one", 2 => "two"}.

--------------------------------------------------------------------------------

(source_file
  (map_expr
    (map_field
      (integer)
      (string))
    (map_field
      (integer)
      (string))))

================================================================================
map with tuple keys
================================================================================

#{{x, y} => "coords", {a, b} => "pair"}.

--------------------------------------------------------------------------------

(source_file
  (map_expr
    (map_field
      (tuple
        (atom)
        (atom))
      (string))
    (map_field
      (tuple
        (atom)
        (atom))
      (string))))

================================================================================
nested map
================================================================================

#{outer => #{inner => value}}.

--------------------------------------------------------------------------------

(source_file
  (map_expr
    (map_field
      (atom)
      (map_expr
        (map_field
          (atom)
          (atom))))))

================================================================================
map update syntax
================================================================================

Map#{key := value}.

--------------------------------------------------------------------------------

(source_file
  (map_expr_update
    (var)
    (map_field
      (atom)
      (atom))))

================================================================================
map update with multiple entries
================================================================================

Map#{a := 1, b := 2}.

--------------------------------------------------------------------------------

(source_file
  (map_expr_update
    (var)
    (map_field
      (atom)
      (integer))
    (map_field
      (atom)
      (integer))))

================================================================================
map pattern match
================================================================================

#{name := Name} = #{name => "Bob"}.

--------------------------------------------------------------------------------

(source_file
  (match_expr
    (map_expr
      (map_field
        (atom)
        (var)))
    (map_expr
      (map_field
        (atom)
        (string)))))

================================================================================
map with mixed entry types
================================================================================

#{a => 1, b := 2}.

--------------------------------------------------------------------------------

(source_file
  (map_expr
    (map_field
      (atom)
      (integer))
    (map_field
      (atom)
      (integer))))

================================================================================
map with list value
================================================================================

#{items => [1, 2, 3]}.

--------------------------------------------------------------------------------

(source_file
  (map_expr
    (map_field
      (atom)
      (list
        (integer)
        (integer)
        (integer)))))

================================================================================
map with binary key
================================================================================

#{<<"key">> => value}.

--------------------------------------------------------------------------------

(source_file
  (map_expr
    (map_field
      (binary
        (bin_element
          (string)))
      (atom))))
