[package]
categories = ["parsing", "text-editors"]
description = "erlang grammar for the tree-sitter parsing library"
repository = "https://github.com/samgaw/tree-sitter-erl"
edition = "2018"
keywords = ["incremental", "parsing", "erlang"]
license = "Apache-2.0"
name = "tree-sitter-erl"
version = "0.16.0"

build = "bindings/rust/build.rs"
include = [
  "LICENSE",
  "bindings/rust/*",
  "grammar.js",
  "queries/*",
  "src/*",
]

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter-language = "0.1.0"

[build-dependencies]
cc = "1.1.10"

[dev-dependencies]
tree-sitter = "0.26.3"
